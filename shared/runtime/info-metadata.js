/* Presentation definitions shared by comparison and checkpoint analytics. */
(function(global){
  'use strict';
  const field=(label,description,behavior,limits)=>({label,description,info:{sections:[{title:'How it is used',text:behavior},...(limits?[{title:'Interpretation',text:limits}]:[])]}});
  const parameters={
    scenario:field('Scenario','The synthetic payment pattern to generate.','The same generated events are supplied to every compared model. Changing the scenario starts a new replay; it does not train model weights.','Imported payment datasets retain their own events and disable this generator setting.'),
    size:field('Network size','The synthetic account population and approximate event count.','Larger networks create more accounts and payment activity, increasing replay and analysis work.','This controls generated data rather than a model’s hidden dimension.'),
    seed:field('Dataset seed','The random seed used to generate a reproducible synthetic dataset.','The same scenario, size, delays, and seed reproduce the same generated events.','Model weights and historical training data remain fixed.'),
    decisionPolicy:field('Decision policy','The method that converts a model score into a block or allow decision.','Unlabeled budget learns a cutoff from warm-up scores; fixed threshold uses the supplied cutoff; cost and objective tuning select a cutoff using saved historical validation outcomes.','Policies select thresholds independently of model weights. Historical tuning requires a compatible validation sample.'),
    alpha:field('Target block rate α','The target fraction of assessed payments placed in the high-score tail.','A shared-budget policy learns a separate cutoff for each model and adapts it toward this target. The comparison also uses α as its ranking evaluation budget.','This is a target rather than a guaranteed realized block rate. It is not an individual payment’s fraud probability.'),
    warmup:field('Warm-up payments','The number of initial payment scores used to initialize an unlabeled cutoff.','Warm-up payments are allowed and excluded from assessed policy metrics. Once enough scores have arrived, the upper-tail quantile sets the first cutoff.','A warm-up longer than the dataset can leave no assessed payments. Fixed and historically fitted cutoffs do not use this warm-up.'),
    manualTau:field('Fixed threshold τ','A cutoff expressed in the selected model’s displayed score units.','Payments with score strictly greater than τ are blocked; scores equal to τ are allowed. The fixed policy keeps this cutoff unchanged throughout replay.','For supervised probability scoring, bits = −log₂(1 − p), so τ = 1 corresponds to p = 0.5. Scores from other heads can have different meanings.'),
    falseBlockCost:field('False-block cost','The relative penalty assigned to blocking a legitimate labeled payment.','Cost tuning minimizes false-block cost × false positives + missed-fraud cost × false negatives on saved historical validation outcomes.','Only the ratio between the two costs affects the selected cutoff. These are relative penalties, not automatically currency amounts.'),
    missedFraudCost:field('Missed-fraud cost','The relative penalty assigned to allowing a fraudulent labeled payment.','Increasing this relative to false-block cost puts more weight on catching fraud when selecting the historical cutoff.','The fitted cutoff is frozen during evaluation; evaluation outcomes do not retune it.'),
    objective:field('Historical objective','The validation metric maximized when selecting an automatic cutoff.','F1 balances precision and recall. F2 gives more weight to recall. Balanced accuracy averages fraud recall and legitimate-payment specificity. Ties prefer fewer blocks.','The metric is evaluated on saved historical validation outcomes, not the currently inspected payment.'),
    policyScope:field('Policy scope','Whether policy settings are shared or configured per model.','Shared unlabeled budget uses one target rate with separately learned cutoffs. Individual settings preserve each model’s policy. Auto-tune fits every model to the selected historical objective.','Sharing a target rate does not force identical numerical thresholds.'),
    trainingMode:field('Model training mode','The saved scoring mode used by the selected checkpoint.','No-label mode scores unusual payment activity without fraud labels. Fraud-label mode uses a historically trained fraud classifier.','Changing this control selects saved scoring behavior; it does not train on the current dataset.'),
    mode:field('Comparison protocol','Which payment history each model observes after policy decisions.','Same-history mode records decisions while allowing payments to remain in the common observed history. Enforced mode removes blocked transfers from that model’s subsequent settled history.','With enforcement, models can develop different later histories because their earlier decisions differ.'),
    predictionHead:field('Prediction head','The scoring rule applied to a native model’s representation or logit.','A historical empirical-tail head ranks link likelihood against frozen reference transactions; likelihood scoring uses the native link likelihood; the fraud head produces a supervised fraud estimate.','Available heads depend on the loaded checkpoint and training mode. A likelihood or rarity score is not a fraud probability.'),
    model:field('Inspected model','The checkpoint whose details, state, and per-model settings are displayed.','All available models continue to participate in the comparison. Selecting one changes the inspector and, under individual settings, the policy controls.','Checkpoint identity identifies the actual saved weights used for results.'),
    forwardDelay:field('Relay interval','Minutes between successive synthetic forwarding payments.','Short intervals create faster relay behavior and change the earlier history available at each request.','This is a synthetic scenario timing parameter; it is disabled for imported events.'),
    reportDelay:field('Report delay','Hours between a synthetic fraud event and its delayed confirmation.','Changing the delay changes when reports become visible during scenario replay.','The original event outcome remains separate from the time its confirmation appears.'),
    partition:field('Evaluation data','The population used to compare saved training runs.','Common held-out test uses the intersection of the selected runs’ test rows. All rows evaluates a separate dataset only after checks exclude training or validation overlap.','Every model is compared on the same transaction IDs; unknown outcomes do not contribute to outcome metrics.'),
    dataset:field('Saved dataset','The stored dataset version supplying comparison transactions.','Saved feature datasets include their selected numeric feature columns. Each trained model must receive its required columns.','Selecting a different dataset does not retrain the saved models.'),
    tree:field('Inspected tree','The individual boosted tree whose path is shown for this payment.','Each split compares a transformed model input with its stored threshold. The reached leaf is multiplied by the checkpoint learning rate before contributing to the raw margin.','Tree selection changes the explanation display, not the ensemble prediction.'),
    learning_rate:field('Tree learning rate','The multiplier applied to each tree’s leaf contribution.','The checkpoint raw margin combines its intercept with the sum of learning-rate-weighted tree outputs.','This is a saved training parameter; the analytics page does not modify it.'),
    probability_threshold:field('Saved probability cutoff','The frozen fraud-probability threshold selected for this training run.','Predictions above this threshold are blocked during the saved comparison.','It is displayed on the probability scale even when the underlying model stores a bit-score cutoff.'),
  };
  const explanations={
    attribution:{title:'Feature attribution',text:'SHAP contributions are measured in raw model margin (log-odds). A positive contribution raises this payment’s output relative to the fixed historical reference; a negative contribution lowers it. Reference margin plus all contributions equals the raw margin. Contributions describe this checkpoint’s behavior and are not causal effects.'},
    population:{title:'Displayed population',text:'Statistics use every payment in the current filtered population. Mean absolute SHAP measures contribution magnitude, while signed mean SHAP retains direction. Plot samples are deterministic and do not change the statistics.'},
    split:{title:'Tree split',text:'The threshold is compared with the transformed model input, not the readable raw value. Inputs less than or equal to the threshold follow the left branch; larger inputs follow the right branch.'},
    native:{title:'Native checkpoint',text:'These saved model and scoring settings describe the Python checkpoint used for the current comparison. Changing the selected evaluation dataset does not refit its weights or training normalization.'},
  };
  // Native persisted settings: meanings follow dyg_tami.DEFAULTS and
  // dyg_tami_fraud.FRAUD_DEFAULTS; public training schemas can enrich these.
  const nativeParameters={
    time_feat_dim:field('Time encoding dimension','The number of learned components used to encode elapsed interaction time.','Time encodings are one input channel of the temporal transformer. Larger dimensions increase representation size and work.'),
    channel_embedding_dim:field('Channel embedding dimension','The width of each projected node, edge, time, and neighbor-co-occurrence channel.','The four channels are concatenated for attention. Four times this dimension must be divisible by the number of attention heads.'),
    patch_size:field('History patch size','The number of neighboring sequence entries combined into one transformer patch.','Larger patches reduce the number of attention tokens. The maximum sequence length must be divisible by the patch size.','Changing this value changes the checkpoint architecture and requires retraining.'),
    num_layers:field('Transformer layers','The number of temporal transformer encoder layers.','Additional layers increase model depth, parameter count, and inference work.'),
    num_heads:field('Attention heads','The number of parallel attention heads in each temporal transformer layer.','Heads attend to different learned parts of the combined channel representation. Four times the channel embedding dimension must be divisible by this value.'),
    dropout:field('Training dropout','The fraction of activations randomly omitted by dropout while training.','Dropout regularizes training. The saved model uses evaluation mode, so this random omission is disabled during comparison.'),
    max_input_sequence_length:field('Maximum temporal sequence length','The padded maximum number of sequence entries supplied for each endpoint.','One entry represents the current endpoint; up to this value minus one earlier interactions are retained by the neighbor sampler. Longer histories increase attention work.','Only strictly earlier interactions are eligible. The sequence length must be at least two and divisible by patch size.'),
    gamma:field('Current-state mixing weight γ','The weight assigned to the current interaction embedding when combining it with historical pair memory.','The combination is γ × current embedding + (1 − γ) × historical embedding. Larger values emphasize the current representation.'),
    epochs:field('Maximum training epochs','The maximum number of passes over the training history.','Training can stop before this limit when validation fails to improve for the configured patience. Saved weights come from the best validation epoch.'),
    batch_size:field('Training batch size','The target number of transaction examples accumulated for an optimizer update.','Timestamp groups remain intact to preserve temporal causality, so actual groups can exceed the target.','This controls training updates rather than the comparison page’s rendering batches.'),
    learning_rate:field('Optimizer learning rate','The step size used by the Adam optimizer during native model training.','Larger values make larger weight updates; smaller values may require more training passes.','This is an optimizer setting, distinct from the shrinkage multiplier used by boosted trees.'),
    weight_decay:field('Optimizer weight decay','The nonnegative weight penalty passed to Adam during training.','A positive value regularizes model weights; zero disables this penalty. Saved comparison predictions do not update the weights.'),
    patience:field('Early-stopping patience','The number of consecutive training epochs without improved validation loss before stopping.','The checkpoint restores the epoch with the lowest validation loss. Increasing patience permits more attempts to improve, up to the epoch limit.'),
    seed:field('Model training seed','The random seed for native weight initialization and historical training sampling.','It records the stochastic training setup. It is independent of the comparison’s synthetic dataset seed.','Reproducibility also depends on model implementation, runtime, and device.'),
    device:field('Native model device','The computation device used by the stored model configuration, such as cpu or cuda.','Tensor operations and model weights are placed on this device when loaded. The comparison service reports its actual inference device separately.'),
    num_threads:field('CPU thread count','The number of PyTorch CPU threads configured for model operations.','More threads may increase throughput for sufficiently large operations but also add scheduling overhead. This does not change the feature definitions.'),
    encoder_training:field('Encoder training mode','Whether supervised fraud fitting updates the temporal encoder.','Finetune updates the encoder and fraud head. Frozen keeps the temporal encoder fixed while fitting the fraud head.','Both modes use historical supervised outcomes only in training losses, not as transaction inputs.'),
    prediction_head:field('Saved fraud-head specification','The registered supervised prediction head and its optional parameter configuration.','The head maps the temporal transaction representation to a fraud logit. The current built-in fraud_linear head uses a learned linear classifier.'),
    class_weight:field('Fraud-class loss weight','The positive-class multiplier used in the supervised binary cross-entropy loss.','None uses a weight of one. Balanced uses the ratio of known legitimate to fraud training outcomes. A positive number supplies an explicit fraud weight.','This changes the fitting loss; the decision policy’s false-block and missed-fraud costs select a cutoff separately.'),
    training_label_cutoff:field('Training label availability cutoff','The latest availability time, in dataset seconds, at which a known outcome can enter supervised training.','An empty value uses the last training transaction time. Earlier transactions with unavailable outcomes can remain temporal context without contributing to the supervised loss.','Retrospective outcomes without recorded availability time follow the dataset’s retrospective-label convention.'),
    validation_label_cutoff:field('Validation label availability cutoff','The latest availability time, in dataset seconds, at which a known outcome can enter validation loss.','An empty value uses the last validation transaction time. Validation labels arriving after the cutoff are excluded from that validation loss.','This controls label availability, not which prior interactions can be used as temporal context.'),
  };
  const readouts={
    'Native fraud logit':{title:'Native fraud logit',description:'The supervised classifier’s raw signed output before the sigmoid converts it to an estimated fraud probability.',sections:[{title:'Scale',text:'A logit of zero maps to probability 0.5. Larger logits produce larger fraud estimates. The estimate is not claimed to be calibrated.'}]},
    'Estimated fraud probability':{title:'Estimated fraud probability',description:'The sigmoid of the historically trained fraud classifier’s logit. It is separate from the policy threshold and the observed outcome.'},
    'Estimated flagged-fraud probability':{title:'Estimated flagged-fraud probability',description:'The saved supervised classifier’s estimate for the proposed payment, using the available earlier history. A model estimate is distinct from the eventual known label.'},
    'Fraud score':{title:'Fraud score',description:'The supervised probability mapped to bits by −log₂(1 − p). Higher values indicate a larger estimated fraud probability.'},
    'Fraud-risk score':{title:'Fraud-risk score',description:'The supervised probability mapped to bits by −log₂(1 − p). The decision policy compares this score with its current cutoff.'},
    'Native link logit':{title:'Native link logit',description:'The frozen temporal model’s raw compatibility score for the proposed source and destination given earlier interaction history.',sections:[{title:'Interpretation',text:'This is an observed-link likelihood signal. It is not a fraud logit and does not use the payment’s outcome.'}]},
    'Native link likelihood':{title:'Native link likelihood',description:'The sigmoid of the native link logit. It describes compatibility with historical interactions rather than fraud probability.'},
    'Likelihood / historical rank':{title:'Likelihood or historical rank',description:'The selected no-label prediction head converts native link likelihood into its displayed probability-like quantity and score.',sections:[{title:'Historical reference',text:'The empirical-tail head compares against frozen reference transactions. Low likelihood or rank means unusual payment activity rather than confirmed fraud.'}]},
    'Head reference':{title:'Prediction-head reference',description:'The number of frozen historical transactions used by the selected scoring head. The current evaluation dataset does not refit this reference.'},
    'Prediction head':parameters.predictionHead.info,
    'Prospective evaluation':{title:'Prospective evaluation membership',description:'Whether this transaction belongs to the model’s evaluated population or supplies earlier context only. Context transactions can update temporal history without contributing to comparison metrics.'},
    'Causal feature vector':{title:'Checkpoint feature inputs',description:'The numeric inputs presented to this saved checkpoint for the proposed payment.',sections:[{title:'History convention',text:'Bundled browser checkpoint features retain their original settled-payment history contract. Dataset-owned materialized features have their own recorded definitions and strictly earlier observed-attempt history.'}]},
    'No-label rarity score':{title:'No-label feature rarity',description:'A score measuring how unusual causal payment features appear against the checkpoint’s historical feature distributions. The supervised fraud probability is not used in this mode.'},
    'Recipient':{title:'Recipient input',description:'The proposed recipient identity is scored against the sender’s earlier interaction context. This categorical input is available before the payment settles.'},
    'Amount bin':{title:'Checkpoint amount category',description:'The categorical amount outcome scored by this checkpoint’s likelihood head.',sections:[{title:'Category calculation',text:'The zero-based integer category counts saved amount boundaries less than or equal to the requested EUR amount. The displayed bin number is that category plus one. The category is not divided by the number of boundaries.'},{title:'Score contribution',text:'The amount head assigns a probability to each category. This row contributes −log₂ of the selected category’s probability to the no-label anomaly score.'}]},
    'Sender activity gap':{title:'Checkpoint timing category',description:'The categorical timing outcome scored by this checkpoint’s likelihood head, based on minutes since the sender’s last observed payment or deposit.',sections:[{title:'Category calculation',text:'For a sender with earlier activity, the zero-based integer category counts saved gap boundaries less than or equal to the nonnegative elapsed minutes. It uses the raw gap in minutes, without a logarithmic transform.'},{title:'First activity',text:'A sender without earlier activity has no measured gap (null). The timing head uses a separate first-activity category at index number of gap boundaries plus one; it is distinct from a measured gap of zero minutes. Outcome reports do not reset the gap.'},{title:'Score contribution',text:'This row contributes −log₂ of the timing head’s probability for the selected category to the no-label anomaly score.'}]},
  };
  readouts['Prediction head']={title:parameters.predictionHead.label,description:parameters.predictionHead.description,...parameters.predictionHead.info};
  function attachControl(root,id,key,overrides={}){
    const control=root.querySelector('#'+id),definition=parameters[key],info=global.PaymentInfo;
    if(!control||!definition||!info)return;
    const host=control.closest('label')||control;
    let heading=host.querySelector('.payment-info-control-heading');
    if(host!==control&&!heading){
      heading=global.document.createElement('span');heading.className='payment-info-control-heading';
      while(host.firstChild&&host.firstChild!==control)heading.appendChild(host.firstChild);
      host.insertBefore(heading,control);
    }
    host.dataset.infoParameter=key;
    info.attach(host,()=>{
      const value=control.tagName==='SELECT'?control.selectedOptions[0]?.textContent:control.value;
      const constraints=Object.fromEntries(['min','max','step'].filter(name=>control.getAttribute(name)!==null).map(name=>[name,control.getAttribute(name)]));
      const options=control.tagName==='SELECT'?Array.from(control.options,option=>({value:option.value,label:option.textContent})):undefined;
      const result=info.parameter({name:key,type:options?'select':control.type,...constraints,...(control.defaultValue?{default:control.defaultValue}:{}),...(options?{options}:{}),...definition,...overrides});
      return {...result,facts:[...(result.facts||[]),['Displayed value',value??''],...(control.disabled?[['Availability','Inactive under the current settings']]:[])]};
    },heading?{buttonParent:heading}:{});
  }
  function feature(definition,facts=[],context){
    const result=global.PaymentInfo.feature(definition),section=explanations[context];
    return {...result,facts:[...(result.facts||[]),...facts],sections:[...(result.sections||[]),...(section?[section]:[])]};
  }
  let modelSchemas=[];
  function setModelSchemas(schemas){modelSchemas=schemas||[];global.dispatchEvent?.(new CustomEvent('payment-info-model-schemas'));}
  function modelFields(id){return modelSchemas.find(model=>model.id===id)?.parameters||[];}
  global.PaymentInfoMetadata={parameters,nativeParameters,explanations,readouts,attachControl,feature,setModelSchemas,modelFields};
})(typeof globalThis!=='undefined'?globalThis:window);
